sonar.projectKey=flask_password
sonar.projectName=flask_password
sonar.projectVersion=1.0
sonar.sourceEncoding=UTF-8

# Source paths
sonar.sources=.
sonar.exclusions=**/.venv/**,**/__pycache__/**,**/static/**,**/templates/**

# Language hints
sonar.python.version=3.11

# Optional: test and coverage (set paths below if you generate them)
# sonar.tests=tests
# sonar.python.coverage.reportPaths=coverage.xml

# Optional: linter reports (Pylint SARIF or generic)
# sonar.python.pylint.reportPaths=pylint-report.txt